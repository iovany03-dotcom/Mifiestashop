(()=>{const button=document.querySelector('.neon-switch');if(!button)return;button.addEventListener('click',()=>{const on=button.getAttribute('aria-pressed')!=='true';button.setAttribute('aria-pressed',String(on));document.body.classList.toggle('neon-on',on);document.querySelector('.neon-before').setAttribute('aria-hidden',String(on));document.querySelector('.neon-after').setAttribute('aria-hidden',String(!on));button.lastElementChild.textContent=on?'Volver al ambiente inicial':'Enciende el ambiente';});})();
