(()=>{const dialog=document.querySelector('.gallery-dialog');if(!dialog)return;const buttons=[...document.querySelectorAll('.gallery-photo')];const img=dialog.querySelector('img');let index=0;function show(i){index=(i+buttons.length)%buttons.length;const source=buttons[index].querySelector('img');img.src=source.src;img.alt=source.alt;dialog.querySelector('.gallery-count').textContent=(index+1)+' / '+buttons.length;}buttons.forEach((b,i)=>b.addEventListener('click',()=>{show(i);dialog.showModal();}));dialog.querySelector('.gallery-close').onclick=()=>dialog.close();dialog.querySelector('.gallery-prev').onclick=()=>show(index-1);dialog.querySelector('.gallery-next').onclick=()=>show(index+1);dialog.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();show(index-1);}if(e.key==='ArrowRight'){e.preventDefault();show(index+1);}});dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close();});})();
